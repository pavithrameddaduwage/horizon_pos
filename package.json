{
  "name": "horizon_pos_portal",
  "version": "1.0.0",
  "private": true,
  "description": "Horizon POS Ingestion Portal - NestJS Backend & Angular Frontend",
  "scripts": {
    "start": "npm run start:backend",
    "start:backend": "npm --prefix backend run start:dev",
    "start:frontend": "npm --prefix frontend run start",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "build:all": "npm --prefix backend run build && npm --prefix frontend run build",
    "build": "npm run build:all"
  }
}
